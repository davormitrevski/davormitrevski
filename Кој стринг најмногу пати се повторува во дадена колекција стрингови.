class Program
    {
        string MaxStr(List<string> str)
        {
            List<string> unikat = new List<string>();
            int max = 0;
            string maxstr = str[0];
            for (int i = 0; i < str.Count; i++)
            {
                bool goima = false;
                for (int j = 0; j < unikat.Count; j++)
                {
                    if (str[i] == unikat[j])
                    {
                        goima = true;
                    }
                }
                if (goima == false)
                {
                    unikat.Add(str[i]);
                }
            }
            for (int j = 0; j < unikat.Count; j++)
            {
                int brojac = 0;
                for (int i = 0; i < str.Count; i++)
                {
                    if (unikat[j] == str[i])
                    {
                        brojac = brojac + 1;
                    }
                    
                }
                if (brojac > max)
                {
                    max = brojac;
                    maxstr = unikat[j];
                }
            }
            return maxstr;
        }
        static void Main(string[] args)
        {
            List<string> str = new List<string> { " eden", "dva", "tri", "dva", "eden", "dva" };
            string maxstr;
            Program obj = new Program();
            maxstr = obj.MaxStr(str);
            Console.WriteLine(" Najmnogu pati se povtoruva stingot: " + maxstr);

        }
    }
}
