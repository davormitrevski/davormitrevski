class Program
    {
        static void Main(string[] args)
        {
            List<string> kolekcija = new List<string> { "eden", "dva", "dva", "tri", "eden", "dva" };
            List<string> unikati = new List<string>();
            for (int i = 0; i < kolekcija.Count; i++)
            {
                bool Have = false;
                for (int j = 0; j < unikati.Count; j++)
                {
                    if (kolekcija[i] == unikati[j])
                        Have = true;
                }
                if (Have == false)
                    unikati.Add(kolekcija[i]);
            }
            int max = 0;
            string str = "";
            for (int i = 0; i < unikati.Count; i++)
            {
                int brojac = 0;
                for (int j = 0; j < kolekcija.Count; j++)
                {
                    if (unikati[i] == kolekcija[j])
                        brojac = brojac + 1;
                }
                if (brojac > max)
                {
                    max = brojac;
                    str = unikati[i];
                }
            }
            Console.WriteLine(str);
            Console.WriteLine(max);
        }
    }
}
